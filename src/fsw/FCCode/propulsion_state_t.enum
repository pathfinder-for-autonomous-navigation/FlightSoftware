#ifndef propulsion_state_t_enum_
#define propulsion_state_t_enum_

// prop.state
enum class propulsion_state_t {
    idle,
    awaiting_pressurization,
    pressurizing,
    firing,
    venting
};

#endif
