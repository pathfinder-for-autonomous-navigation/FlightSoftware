#ifndef radio_state_t_enum_
#define radio_state_t_enum_

enum class radio_state_t {
  disabled,
  wait,       // IDLE
  transceive, // SBDIX 
  read,       // SBDRB
  write,      // SBDWB
  config      // CONFIG
};

#endif
