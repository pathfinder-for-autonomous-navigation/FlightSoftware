#ifndef radio_mode_t_enum_
#define radio_mode_t_enum_

enum class radio_mode_t {
  transceive, // SBDIX
  wait, 
  read,       // SBDRB
  write,      // SBDWB
  config,
  manual,     // Don't use state machine
};

#endif
