#ifndef mission_state_t_enum_
#define mission_state_t_enum_

// pan.state
enum class mission_state_t {
    startup,
    detumble,
    initialization_hold,
    follower,
    standby,
    leader,
    docking,
    docked,
    paired,
    spacejunk,
    safehold,

    manual
};

#endif
