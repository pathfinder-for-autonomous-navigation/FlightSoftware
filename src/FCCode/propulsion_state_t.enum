#ifndef propulsion_state_t_enum_
#define propulsion_state_t_enum_

// prop.state
enum class prop_state_t {
    idle,
    pressurizing,
    firing,
    handling_fault // state where we handle faults
};

#endif
